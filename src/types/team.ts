export interface TeamMember {
  id: string;
  name: string;
  role: string;
  symbol: string;
  atomicNumber: number;
  category: string;
  email: string;
  imageUrl?: string;
  backgroundColor?: string;
}
